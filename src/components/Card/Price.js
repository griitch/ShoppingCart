import styled from "styled-components";
import CenteredDiv from "../shared/CenteredDiv";

const Price = styled(CenteredDiv)`
  height: 60px;
  font-weight: 800;
  font-size: 1.1rem;
`;

export default Price;
